import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#0C0E11', // Cambia este color por el que desees
    },
    secondary: {
      main: '#FF0000', // Color secundario
    },
    azulote: {
      main: '#000AFF', // Color amarillo
    },
  },
  
});

export default theme;